(* math_operations.ml *)

(* Définition du module MathOperations *)
module MathOperations = struct
  let add x y = x + y
  let subtract x y = x - y
  let multiply x y = x * y
  let divide x y = x / y
end
